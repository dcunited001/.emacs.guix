# -*- mode: snippet -*-
# contributor: Translated to yasnippet by sublime-snippet import
# key: cisco.ios.ios_ospf_interfaces
# group: cisco.ios
# name: Resource module to configure OSPF interfaces
# --
- name: ${1:Resource module to configure OSPF interfaces}
  cisco.ios.ios_ospf_interfaces:
    config: ${2:# A dictionary of OSPF interfaces options}
    running_config: ${3:# This option is used only with state I(parsed)}
    state: ${4:# The state the configuration should be left in}
