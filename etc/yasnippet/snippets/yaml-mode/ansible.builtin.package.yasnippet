# -*- mode: snippet -*-
# contributor: Translated to yasnippet by sublime-snippet import
# key: ansible.builtin.package
# group: ansible.builtin
# name: Generic OS package manager
# --
- name: ${1:Generic OS package manager}
  ansible.builtin.package:
    name: ${2:# Package name, or package specifier with version}
    state: ${3:# Whether to install (C(present)), or remove (C(absent)) a package}
    use: ${4:# The required package manager module to use (C(yum), C(apt), and so on)}
