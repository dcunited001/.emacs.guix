# -*- mode: snippet -*-
# contributor: Translated to yasnippet by sublime-snippet import
# key: containers.podman.podman_volume_info
# group: containers.podman
# name: Gather info about podman volumes
# --
- name: ${1:Gather info about podman volumes}
  containers.podman.podman_volume_info:
    name: ${2:# Name of the volume}
    executable: ${3:# Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)}
