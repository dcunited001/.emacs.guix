# -*- mode: snippet -*-
# contributor: Translated to yasnippet by sublime-snippet import
# key: ansible.builtin.ping
# group: ansible.builtin
# name: Try to connect to host, verify a usable python and return C(pong) on success
# --
- name: ${1:Try to connect to host, verify a usable python and return C(pong) on success}
  ansible.builtin.ping:
    data: ${2:# Data to return for the C(ping) return value}
  tags:
    - ping