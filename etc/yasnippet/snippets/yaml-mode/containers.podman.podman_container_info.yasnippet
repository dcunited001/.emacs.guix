# -*- mode: snippet -*-
# contributor: Translated to yasnippet by sublime-snippet import
# key: containers.podman.podman_container_info
# group: containers.podman
# name: Gather facts about containers using podman
# --
- name: ${1:Gather facts about containers using podman}
  containers.podman.podman_container_info:
    name: ${2:# List of container names to gather facts about}
    executable: ${3:# Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)}
