# -*- mode: snippet -*-
# contributor: Translated to yasnippet by sublime-snippet import
# key: containers.podman.podman_pod_info
# group: containers.podman
# name: Gather info about podman pods
# --
- name: ${1:Gather info about podman pods}
  containers.podman.podman_pod_info:
    name: ${2:# Name of the pod}
    executable: ${3:# Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)}
  tags:
    - containers
    - podman
    - podman_pod_info